image: python:3.8-alpine

pages:
  stage: deploy
  only:
    - master
  script:
    - apk add py3-pip
    - pip install --upgrade pip
    - pip install ".[docs]"
    - mkdocs build --site-dir public
  artifacts:
    paths:
    - public

